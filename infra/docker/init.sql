-- İlk çalıştırmada tabloyu oluşturmayı JPA'ya bırakacaksan bu dosya şart değil.
-- Sadece kullanıcı/DB zaten environment ile oluşuyor.

-- Örnek: bağlantı testleri için ufak bir şey eklemek istersen:
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
